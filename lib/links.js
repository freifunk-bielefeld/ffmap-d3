var linkcolor_default = d3.scale.linear()
        .domain([1, 0.5, 0])
        .range(["#0a3", "orange", "red"]);

var linkcolor_wifi = d3.scale.linear()
        .domain([1, 0.7, 0])
        .range(["#0a3", "orange", "red"]);
